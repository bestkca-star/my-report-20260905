# 판단 기준

혼자 자기 앱을 만들 때 이 문서만 본다. 도메인 이름은 적지 않는다 — 적으면 그건 예시다.

---

## 2026-09-02 · 앱이 데이터를 거부할 수 있는가

### 배운 것

**차단과 경고를 가르는 것은 "계산이 틀리는가"다.**
틀린다 → 차단. 해석·구현만 조심하면 된다 → 경고.

**검증 규칙은 적게 만든다.** 차단을 늘리면 실무 데이터는 늘 어딘가 깨져 있으므로
앱이 자꾸 멈추고, 사람이 차단을 하나씩 끄기 시작하고, 결국 검증 전체가 무력해진다.

**임계값에는 근거를 적는다.** 근거 없는 임계값은 남의 것이다.
남이 준 숫자를 그대로 쓰면 두 달 뒤 "왜 그 값이죠?"에 답할 수 없다.
연간 건수가 적은 도메인에서 큰 최소표본을 요구하면 어떤 세그먼트도 충족하지 못한다.
낮추되 경고는 없애지 않는다.

**메시지에는 숫자를 넣는다.** "실패"는 판단 재료가 아니다.
실제 값 · 기준값 · (경고라면) 왜 정상일 수도 있는지를 함께 놓는다.
"행 수 부족"으로는 아무것도 판단할 수 없고, "12행 (최소 100)"이면 파일이 잘못 온 것인지
아직 덜 쌓인 것인지 구분된다. 대응이 다르다.

**결측은 오류일 수도 구조일 수도 있다.** 구조면 채우지 않는다. 채우면 위조다.
없는 것이 사실이기 때문이다. 평균이나 0으로 채우면 "그 일이 일어나지 않은 대상"이
"값이 0인 대상"으로 바뀐다. 대신 조인할 때 조용히 사라지므로 그 인원이 어디로 갔는지
알고 있어야 한다.

**검증은 만든 것으로 끝나지 않는다.** 깨뜨려 넣어 실제로 막히는지 본다.
경고만 띄우고 진행되는 검증은 없는 것과 같다. 통과 버튼이 잠기는 것을 눈으로 봐야 한다.
앱이 에러로 죽는 것과 차단으로 멈추는 것은 다르다 — 죽으면 사람이 판단할 화면이 없다.

**아직 안 채운 자리는 에러가 아니라 안내로 만든다.**
한 자리가 비었다고 화면 전체가 사라지면, 이미 채운 자리까지 못 쓴다.

**예외를 잡는 감싸개는 함수 안에서 난 예외만 잡는다.**
인자를 만들다 난 오류는 못 잡는다 — 감싸개를 부르기 전에 이미 터졌기 때문이다.
안전장치를 붙였다고 안심하지 말고, 실제로 그 상황을 만들어 넣어 봐야 한다.

**거버넌스 기록은 뒷단계의 성공 여부와 무관하게 남겨야 한다.**
사람이 판단을 내린 그 순간에 저장한다. 나중에 저장하면, 중간에 무엇 하나가 깨졌을 때
판단한 사실 자체가 사라진다.

### 오늘 내가 내린 결정

| 규칙 | 판정 | 왜 |
|---|---|---|
| 행 수가 최소표본 미만 | **차단** | 그 밑에서는 비율을 내도 신뢰구간이 너무 넓어 판정에 쓸 수 없다. 임계값은 이미 명세에 적어 둔 최소표본을 그대로 가져왔다 |
| 필수 컬럼이 없음 | **차단** | 하나만 없어도 주지표나 퍼널 중 하나가 아예 안 나온다. 없어도 되는 컬럼과는 구분한다 |
| 기간보다 **앞선** 날짜 | **차단** | 뒤 단계는 앞 단계보다 먼저 일어날 수 없다. 지난 기간 파일을 잘못 올린 것이다 |
| 기간보다 **뒤인** 날짜 | **경고** | 후속 단계 일자가 기준 기간보다 뒤인 것은 구조적으로 정상이다. 차단하면 앱이 늘 멈춘다 |
| 완전중복 행 | **경고** | 인원을 고유 식별자로 세면 값이 맞는다. 구현만 조심하면 되는 것이다. 다만 행으로 세면 분모가 부풀어 전환율이 전부 틀어지므로 메시지에 그 사실을 적었다 |

### 오늘 예상이 틀린 곳 — 여기가 배운 것

날짜 범위를 처음에는 **경고 하나**로 정했다. 근거는 "기간 밖 행은 빼고 진행하면 된다"였다.
그런데 날짜를 통째로 한 기간 옮긴 파일을 넣어 보니 **99%가 어긋났는데 경고만 뜨고 통과됐다.**
"그 행만 빼면 된다"는 근거가 그 상황에서는 성립하지 않았다 — 뺄 게 없었다.

**하나의 규칙 안에서도 방향에 따라 판정이 갈릴 수 있다.**
규칙을 만들 때 "이게 깨지는 경우"를 한 가지만 상상하면, 상상하지 않은 쪽이 그대로 통과한다.
깨뜨려 보지 않았으면 몰랐을 것이다.

---

## 2026-09-02 (이어서) · 내 앱의 숫자가 맞는가

### 배운 것

**손계산과 대조하기 전까지는 맞는 숫자가 아니다.**
계산이 돌아간다는 것과 값이 맞다는 것은 다르다. 미리 세어 둔 값이 없으면
표본 100건을 눈으로 세어서라도 자릿수를 맞춰 본다.

**같은 계산식을 두 곳에 적으면 반드시 어긋난다.**
카드에 쓰는 식과 추이에 쓰는 식이 같아야 한다면, 식은 한 곳에만 두고 둘 다 그것을 부른다.

**유효 구간은 지표마다 다르다.**
"이 데이터는 언제부터 언제까지 믿을 수 있나"에 답이 하나일 것 같지만 아니다.
결과가 나오기까지 기다려야 하는 지표, 관측 창이 닫혀야 하는 지표, 시점에 바로
확정되는 지표는 잘리는 끝이 각각 다르다. 하나로 뭉뚱그리면 어느 하나는 틀린다.

**"최근이라 낮다"는 확인하기 전에는 추측이다.**
관측 기간이 짧아 낮아 보이는 것과 실제로 나빠진 것은 다르고, 둘은 **재 봐야** 갈린다.
다음 단계까지 걸리는 시간을 재고, 그만큼 시간이 지난 대상만으로 다시 비교한다.
확인해 보니 반대인 경우도 있다 — 오래된 쪽이 표본 부족이었다.

**잘라야 할 곳이 최근이라는 보장은 없다.**
가장 오래된 구간이 미달일 수 있다. 시작 지점도 끝 지점만큼 의심한다.

**비교 기간이 칸마다 다르면, 나란히 놓인 숫자들이 서로 다른 것을 말한다.**
어느 칸에 값이 없다고 그 칸만 한 칸 건너뛰어 비교하면 안 된다. 비교를 포기하고
왜 못 하는지를 적는 편이 낫다.

**판정이 양방향인데 판정 함수가 한쪽만 보면, 나머지 절반은 조용히 통과한다.**
"정상"이라고 적는 화면에는 그 판정이 어디까지 동작하는지도 같이 적는다.

**표시 형식은 값을 바꿔주지 않는다.**
단위 기호를 붙인다고 값이 그 단위로 변환되지는 않는다. 막대 길이는 맞는데
라벨만 틀린 표는, 틀린 것을 눈치채기가 더 어렵다.

**폐기 예정이라고 적힌 것은 기한이 지났는지 본다.**
경고가 뜨는데 아직 동작한다는 이유로 두면, 배포하는 날 깨진다.

**도구가 흉내내지 못하는 것은 "확인했다"고 말하지 않는다.**
자동 점검이 통과했다는 것과 실제 화면에서 그렇게 동작한다는 것은 다르다.
검증 도구의 한계 때문에 못 본 것은 못 봤다고 말하고, 어떻게 봐야 하는지를 적는다.

**빈칸이 온 요청은 채우지 말고 되묻는다.**
"이건 내가 정한다"고 한 자리가 빈 채로 다시 오면, 그건 채워도 된다는 뜻이 아니라
아직 안 정했다는 뜻이다. 대신 채우면 결정을 가로챈 것이 된다.

**요청에 내 데이터에 없는 이름이 들어 있으면, 만들지 말고 없다고 말한다.**
비슷한 것에 억지로 대응시키면 그 순간부터 모든 숫자가 지어낸 것이 된다.

### 오늘 내가 내린 결정

| 결정 | 무엇으로 | 왜 |
|---|---|---|
| **그레인** | 대상(사람)이 아니라 **한 건의 처리**를 하나로 센다 | 지금은 두 수가 같지만, 같은 대상이 나중에 다시 들어오면 갈라진다. 그때 대상으로 세면 재유입이 사라진다 |
| **세는 법** | 행이 아니라 **고유 식별자**로 센다 | 완전중복 행이 있어 행으로 세면 첫 단계가 부풀고 전환율이 전부 낮아진다 |
| **추이의 기간 단위** | 짧은 단위가 아니라 **긴 단위**로 자른다 | 짧게 자르면 한 칸의 표본이 최소치에 못 미쳐 변동이 전부 노이즈다. 판정 단위와 표시 단위를 맞췄다 |
| **양방향 지표의 증감 색** | 색을 **주지 않는다**(회색) | 오름·내림 어느 쪽도 좋다고 말할 수 없는 지표에 색을 붙이면 한쪽이 좋은 것처럼 읽힌다 |
| **기간 필터의 기준** | 이벤트 일자가 아니라 **시작 시점** | 일자로 자르면 시작은 구간 안이고 끝은 밖인 대상이 통째로 빠져 전환율이 낮게 나온다 |
| **유지의 정의** | ⚠ **아직 정하지 않았다** | 받은 정의가 이 데이터에 없는 개념이었다. 억지로 옮기면 부호가 뒤집혀 좋고 나쁨이 반대로 읽힌다. 비워 둔 채로 뒀다 |

### ★ 말 안 하고 정해진 것 중 잡아낸 것

무엇을 임의로 정했는지 **물어봐서** 드러난 것들이다. 묻지 않았으면 전부 그냥 넘어갔다.

| # | 말 없이 정해져 있던 것 | 무엇이 걸렸나 |
|---|---|---|
| 1 | **기간으로 자르지 않고 전부 셌다** | 자르면 주지표의 분모가 바뀐다. 검증에서 "기간 밖"을 경고로 넘긴 것과, 계산에서 그것을 그대로 쓴 것은 **별개의 결정**인데 하나만 말했다 |
| 2 | **각 단계를 서로 독립으로 셌다** | 앞 단계를 거쳤는지 강요하지 않았다. 지금 데이터는 맞지만, 순서가 깨진 대상이 섞이면 조용히 부풀어 오른다 |
| 3 | **중복을 조용히 무시했다** | 고유값으로 세면 결과는 맞지만, 계산 결과만 봐서는 중복이 있었는지 알 수 없다 |
| 4 | **단계 이름이 데이터에 있는지 검사하지 않았다** | 이름이 하나 틀리면 그 단계가 0이 되고 전환율이 0%로 나온다. 에러가 아니라 **그럴듯한 숫자**로 나온다 |
| 5 | **첫 칸의 빈 값을 0으로 채웠다** | 옆 칸은 비워 뒀는데 이 칸만 0이라 일관되지 않다. 없는 것을 0으로 적는 건 위조에 가깝다 |
| 6 | **동률일 때 하나만 골랐고, 그것도 빈 값에 기대고 있었다** | 명시적으로 뺀 게 아니라 빈 값이 저절로 건너뛰어지는 성질에 기댄 것이라 약하다 |
| 7 | **정의를 계산 파일이 아니라 설정 파일로 옮겼다** | 더 나은 자리일 수 있지만, 지시받은 것과 다른 자리다. 말하지 않으면 다음 사람이 못 찾는다 |

**요령**: 만든 쪽에 "네가 임의로 정한 것을 전부 말해봐"라고 묻는다.
그리고 답을 **결과가 달라지는 것**과 **표시만 달라지는 것**으로 나눠 달라고 한다.
앞쪽만 보면 된다.

---

## 2026-09-03 · 무엇을 보여주지 않을 것인가

### 배운 것

**못 믿을 조건이면 계산하지 않는다. 계산해 놓고 숨기는 것이 아니다.**
값이 변수에 들어 있으면 화면에서 뺐어도 문서나 로그로 새어 나간다.
손에 없으면 못 쓴다. 그게 설계다.

**사람은 본 숫자를 기억한다. 회색으로도 보여주지 않는다.**
회의실에서 인용되는 것은 경고가 아니라 숫자다. 대신 왜 못 믿는지는 자세히 적는다.

**감출 때도 조건 값은 보여준다.**
표본 수·기간·비중은 "이 판단이 왜 막혔는지"를 설명하는 재료라 감추지 않는다.
감추는 것은 지표 값·증감·유의성이다. 둘을 섞으면 사유가 두루뭉술해진다.

**사유에는 실제 숫자를 넣는다.**
"표본 부족"이 아니라 "표본 179건 (최소 200)". 두루뭉술하면 사람이 판단할 수 없고,
얼마나 모자란지 모르면 기준을 낮춰 통과시키고 싶어진다.

**사유만 적고 끝내면 사람이 우회하려 한다.**
"무엇을 하면 믿을 수 있는지"까지 적는다. 얼마나 더 모이면 되는지, 얼마나 기다리면
되는지, 아니면 더 굵게 묶으면 되는지 — 길이 보이면 기준을 흔들지 않는다.

**조건은 여럿이어도 분기는 하나다.**
조건마다 분기를 만들면 나중에 하나를 빠뜨린다. 결론이 "이 값으로는 판단할 수 없다"로
같으므로 갈림길도 하나면 된다.

**판정 순서를 코드로 박는다.**
좋은 결과를 먼저 보면 뒤의 확인을 대충 한다. 이건 사람의 문제라 규율로는 안 지켜진다.
주지표 확인을 가드레일보다 앞에 두되, 통과했다고 거기서 끝내지 않는다.

**주지표가 움직였다고 성공이 아니다. 무엇을 희생했는지 함께 본다.**
가드레일을 못 봤으면 그 사실을 카드에 적는다. 비워두고 성공이라 하지만 않으면 된다.
확인 못 한 것을 확인한 것처럼 적는 것이 가장 나쁘다.

**움직임의 기준은 평소 변동 폭을 재서 정한다.**
얼마가 의미 있는 변화인지는 데이터가 알려준다. 평소 오르내리던 폭보다 커야
"움직였다"고 할 수 있다. 그 폭을 모르면 먼저 재야 한다.

**쪼개는 축은 격차가 보이는 것이 아니라 손을 쓸 수 있는 것으로 고른다.**
나눠서 차이가 커도 우리가 못 바꾸는 것이면 할 일이 없다.
격차가 없는 것도 결과다 — 그 축으로는 안 갈린다는 것을 확인한 것이다.

**칸을 잘게 나눌수록 표본이 준다.**
축을 고르는 것과 최소 표본을 정하는 것은 한 몸이다. 다 감춰지면 기준이 높은 것이고,
하나도 안 감춰지면 낮은 것이다. 기준을 낮춰 통과시키는 것이 가장 나쁘다 —
판정은 나오는데 믿을 수 없는 판정이다.

**전체와 부분의 합을 대조한다.**
쪼갠 칸의 합이 전체와 다르면 분류가 없는 대상이 빠진 것이다.
조용히 빼지 말고 몇 건인지 남긴다. 그것이 30%면 분해 결과 전체가 의미를 잃는다.

**색은 판정을 전달한다. 장식에 쓰지 않는다.**
강조 색이 "이걸 봐야 한다"는 뜻이 아니게 되면, 진짜 봐야 할 때 아무도 안 본다.
색만으로 전달하지 않는다 — 기호와 글자를 같이 써야 색을 못 봐도 전달된다.

**시킨 파일 밖이 바뀌었는지 본다.**
대개 선의로 고친 것이지만, 그중에 오늘 만든 순서가 섞여 있을 수 있다.
바꿨으면 말한다. 말없이 고친 것은 고치지 않은 것보다 나쁘다.

**기준을 적은 문서는 코드보다 빨리 낡는다.**
"미정"이라고 적힌 것 중 이미 정해진 것이 있는지 먼저 본다.
안 그러면 다음 사람이 그걸 미정으로 알고 시작한다.

### 오늘 내가 내린 결정

| 결정 | 무엇으로 | 왜 |
|---|---|---|
| **쪼갤 축** | 격차가 가장 큰 축 하나 | 격차가 커서가 아니라 **배분·일정으로 손을 쓸 수 있어서**. 더 큰 격차가 보이는 축이 있어도 못 바꾸는 것이면 안 고른다 |
| **보는 구간** | 전체가 아니라 **병목 한 구간** | 어디를 고칠지 정하려는 것이므로 가장 많이 빠지는 구간만 본다 |
| **표본 기준** | 기존 값을 **유지** | 칸 크기와 대조해 검증했다 — 더 낮추면 아무것도 안 걸리고, 더 높이면 한 축이 통째로 감춰진다. 기존 근거가 이미 있어 새로 만들지 않았다 |
| **기간 기준** | 다음 단계까지의 **최대 소요 기간** | 그만큼 안 지난 대상은 "아직 갈 시간이 없는 것"이지 실패한 것이 아니다 |
| **공정성 조건** | 실험이 없으므로 **유효 구간 밖 혼입**으로 대체 | 배정 비율을 볼 실험이 없다. 억지로 셋을 채우지 않고 이 도메인에 있는 것으로 바꿨다 |
| **움직임 기준** | 평소 분기간 변동의 **최대치 수준** | 중앙값보다 크게 잡아 평소 오르내림을 "변화"로 읽지 않게 했다 |
| **판정 이름** | "성공"이 아니라 **"주목할 만함"** | 주지표가 **양방향**이라 오른 것이 좋은 것이 아니다. 이름이 판정을 왜곡하면 안 된다 |
| **감춘 칸의 표시** | 사유 + 표본 + 비중, 그리고 **"왜 감췄나" 모달** | 카드에 다 적으면 지저분하고, 안 적으면 우회하려 한다. 조건 값만 모달로 뺐다 |

### ★ 말 안 하고 정해진 것 중 잡아낸 것

| # | 말 없이 정해져 있던 것 | 무엇이 걸렸나 |
|---|---|---|
| 1 | **시키지 않은 파일이 함께 바뀌었다** | 필요한 변경이었지만 **말하지 않았다.** 같은 함수를 부르는 다른 화면이 있어 안 고치면 깨지는 상황이었는데, 그 사실을 먼저 알렸어야 했다 |
| 2 | **함수를 갈아끼우며 옆 블록을 통째로 삼켰다** | 바꿀 범위를 문자열 위치로 잡았더니 그 사이의 다른 함수들이 같이 지워졌다. 화면이 죽고 나서야 알았다 — **되돌릴 곳이 있어서 살았다** |
| 3 | **판정용 계산과 표시용 계산을 구분하지 않았다** | "계산하지 않는다"가 어디까지인지 모호했다. 막는 것은 *이 비교의 판정*이고, 이미 화면에 쓰는 값까지 막는 것은 아니다. 경계를 말로 적어야 했다 |
| 4 | **안내 문구에 무의미한 수를 넣었다** | 반올림 때문에 "약 0개월"로 나왔다. 계산은 맞았지만 **읽는 사람에게 아무 뜻이 없는 숫자**였다. 단위를 바꿔야 했다 |
| 5 | **기록 파일을 지우는 정리 명령을 습관처럼 붙였다** | 판단 근거가 담긴 기록이 사라졌다. **한 번 겪고 적어 두었는데 또 반복했다.** 버전 관리가 안 되는 파일은 지우는 명령을 아예 쓰지 않는 것이 맞다 |

**요령**: 무엇을 바꿨는지 물을 때 "시킨 것 말고 바뀐 것"을 따로 물어본다.
그리고 지우는 명령은 **파일 이름을 직접 적어서만** 쓴다. 패턴으로 지우지 않는다.
