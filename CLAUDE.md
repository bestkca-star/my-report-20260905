# my-report

**방문검진(출장 건강검진) 결과의 심사유의 판정을 분석해, 보험사 담당자에게 검진 프로세스와 판정 운영의 개선을 제안하는 앱이다.**

> 데이터는 전부 **합성**이다. 실제 보험사 데이터(`tsa-data-work` · `asp_data_2026` ·
> `kb_data_2026` · `#0N. (보험사)`)는 조회하지 않는다. 모수의 출처도 규칙에 걸린다.

---

## 그레인 — 무엇을 하나로 세는가

**검진 건 1회.** 식별자는 `검진건ID` 다.

- 세는 법은 `nunique` 다. `검진이벤트` 에 **완전중복 188행(2.9%)** 이 있어, 행으로 세면
  접수가 2,062로 잡히고 분모가 부풀어 전환율이 전부 낮아진다.
- **사람(`피검자ID`)이 아니다.** 지금 데이터는 피검자 1인 = 검진건 1건이라 값이 같지만,
  재검이 생기면 한 사람이 검진건 2건을 갖게 되어 갈라진다.

| 파일 | 그레인 | 행 |
|---|---|---:|
| `data/검진건.csv` | 검진 건 1회 | 2,004 |
| `data/검진이벤트.csv` | 검진 건 × 단계 도달 1건 | 6,468 |

기간은 `config.PERIOD` = **2025-09-01 ~ 2026-08-31** (`검진건.의뢰일` 기준).

---

## 퍼널 — 4단계

`config.FUNNEL_STEPS` 가 원본이다. 순서가 곧 단계 순서다.

| # | 단계 | 도달 | 전 단계 대비 | 누적 |
|---|---|---:|---:|---:|
| 1 | 검진의뢰접수 | 2,004 | — | 100.00% |
| 2 | 방문예약확정 | 1,794 | 89.52% | 89.52% |
| 3 | 검진수행완료 | 1,525 | 85.01% | 76.10% |
| 4 | **결과전항목수신** | **957** | **62.75%** ← 병목 | 47.75% |

**회신은 단계가 아니다.** 전건에 회신이 오므로 이탈이 없다.

### 각 단계의 도달 조건

현재 코드가 쓰는 조건은 하나다 — **`검진이벤트` 에 그 단계 이름의 행이 있으면 도달**
(`검진건ID` 고유 기준). 앞 단계 부분집합이 4단계 전부에서 성립하는 것은 확인했다.

⚠ **단계별 업무 정의는 미정.** 특히 "결과 **전항목** 수신"에서 *전항목*의 기준이 무엇인지,
일부 항목만 온 건을 어떻게 볼지 정하지 않았다. 지금은 이벤트 행의 유무만 본다.

⚠ **`PERIOD` 로 자를 것인가 미정.** 현재 `funnel()` 은 **자르지 않는다.**
자르면 1,791 / 1,505 / **916** 이 되고, 결과수신이 957에서 **41건 줄어든다.**
이 값은 주지표의 분모라서 심사유의율도 함께 바뀐다.

---

## 지표

### 주지표

**심사유의율 = 심사유의 건 ÷ 결과 수신 건**

- 실측 **36.15%** (346 / 957) · 95% CI 33.11~39.29%
- 분자는 **검진 측 판정**(`검진건.심사유의여부`)이다. 보험사 인수 판정은 쓰지 않는다.
- 분모는 **결과 전항목 수신 건**이다.

### 가드레일

| | 지표 | 계산식 | 실측 |
|---|---|---|---|
| ① | 경계선 비율 | 경계선 건 ÷ 심사유의 건 | 22.83% (분모 346) |
| ② | 건당 유의 항목 수 | **미정** | 미실측 |
| ③ | 검진기관별 유의율 분산 | **미정** | 미실측 |
| ④ | 재방문 출장비 | **미정** — 금액 미정. 손익분기가 아니라 **예산 한도**형이다 | 미실측 |

⚠ **경계선 폭은 잠정 ±10%.** 확정하면 ①의 값(22.83%)이 바뀐다.

`metrics.kpis()` 가 화면에 올리는 것은 **심사유의율 · 경계선 비율 · 재작업률 셋**이다.
②③④는 계산식이 없어 넣지 않았다. `metrics.monthly()` 는 **월이 아니라 분기**로 자른다.

### 유지 / 이탈 (명세 5행)

**재작업률 = 결과 수신 후 90일 이내 재검 발생 건 ÷ 관측 창이 닫힌 결과 수신 건**

- 실측 **9.94%** (69 / 694)
- ⚠ 이름이 "이탈률"이 아니라 **"재작업률"** 인 이유는 **부호가 반대**이기 때문이다.
  검진에서 재검은 나쁜 신호다.
- 관측 창 90일이 안 닫힌 건은 NULL이다. **최근 3개월은 통째로 판정할 수 없다.**

`config.RETENTION_STEPS` 는 아직 비어 있다 — **미정**(Day2에 옮긴다).

---

## 임계값과 근거

### 심사유의율 (양방향)

| | 아래쪽 | 위쪽 |
|---|---|---|
| 경고 | 29% 미만 | 43% 초과 |
| 위험 | 25% 미만 | 47% 초과 |

**근거** — 헛경보율이 4.0%가 되도록 잡은 값이다. 양방향인 이유는, 유의율이 낮아지는 것도
높아지는 것도 판정 운영이 흔들렸다는 신호이기 때문이다.
**판정은 분기 단위로 한다** — 월 수신이 66~100건뿐이라 월별 변동(26.9~48.1%)이 전부 표본
노이즈다(동질성 p=0.271).

`config.THRESHOLDS` 에 들어가 있고, `metrics.status_of()` 가 **양쪽 경계를 모두 읽는다.**
위쪽 경계는 `경고_상한`·`위험_상한` 키로 적는다.

### 최소 표본

`config.MIN_SAMPLE = 200`

**근거** — 명세 4행의 "분기 최소 200건". 이 밑으로는 비율을 내도 신뢰구간이 너무 넓어
판정에 쓸 수 없다. 검증 규칙 1(행 수)이 이 값을 차단 기준으로 쓴다.

### 가드레일 임계값

**전부 미정.** ②③④의 계산식이 정해지지 않아 임계값도 정할 수 없다.

---

## 유효 구간 — 지표마다 다르다

데이터 종료일은 **2026-09-19**(이벤트 최대 일자)다. 시작 쪽은 셋 다 2025-09-01 —
검진건 2,004건 전부가 접수 이벤트를 창 안에 갖고 있어 **좌측 절단이 없다.**

| 지표 | 신뢰 구간 | 기준 축 | 왜 거기서 잘리나 |
|---|---|---|---|
| 퍼널 전환율 | 2025-09-01 ~ **2026-08-16** | 의뢰일 | 의뢰→수신 **최대 34일**. 종료일에서 34일을 뺀 날까지만 "갈 시간이 있었다" |
| 심사유의율 | 2025-09-01 ~ **2026-09-19** | 수신일 | 판정이 **수신 시점에 확정**된다. 기다릴 시간이 없으므로 자르지 않는다 |
| 재작업률 | 2025-09-01 ~ **2026-06-21** | 수신일 | 재검 관측 창 **90일**. 그 뒤 수신 건은 재검 여부가 아직 정해지지 않았다 |

⚠ **분기 판정은 2025Q4부터**다. 2025Q3는 세 지표 모두 `MIN_SAMPLE=200` 미달이다
(퍼널 159 · 수신 25). 가장 오래된 구간이 미달이라는 점에 주의한다.

⚠ **`funnel()` 은 현재 자르지 않는다.** 자르면 결과수신이 957 → 916으로 41건 줄고
주지표의 분모가 바뀐다. 자를지는 미정이다.

## 코드를 고칠 때 지킬 것

- **지표 정의는 `core/config.py` 에서만 바꾼다.** 계산 파일에 값을 박지 않는다.
- **계산 경로에 현재 시각을 넣지 않는다.** 같은 입력이면 같은 결과가 나와야 한다.
- **시키지 않은 파일은 고치지 않는다.**

---

## 아직 안 정한 것 (한자리에)

| | 미정 | 정하면 무엇이 바뀌나 |
|---|---|---|
| 1 | 퍼널을 `PERIOD` 로 자를 것인가 | 결과수신 957 → 916. **주지표 분모가 바뀐다** |
| 2 | "결과 전항목 수신"의 *전항목* 기준 | 4단계 도달 조건 |
| 3 | 경계선 폭 (잠정 ±10%) | 가드레일 ① 값 (22.83%) |
| 4 | 가드레일 ②③④의 계산식 | 그 지표들과 임계값 전부 |
| 5 | 확인용 `st.caption` 3개 삭제 | 브라우저에서 조각 격리를 확인한 뒤 지운다 |
| 6 | `config.RETENTION_STEPS` | 유지 퍼널 화면 |
