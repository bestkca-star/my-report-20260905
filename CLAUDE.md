# my-report

**방문검진(출장 건강검진) 결과의 심사유의 판정을 분석해, 보험사 담당자에게 검진 프로세스와 판정 운영의 개선을 제안하는 앱이다.**

> 데이터는 전부 **합성**이다. 실제 보험사 데이터(`tsa-data-work` · `asp_data_2026` ·
> `kb_data_2026` · `#0N. (보험사)`)는 조회하지 않는다. 모수의 출처도 규칙에 걸린다.

---

## 그레인 — 무엇을 하나로 세는가

**검진 건 1회.** 식별자는 `검진건ID` 다.

- 세는 법은 `nunique` 다. `검진이벤트` 에 **완전중복 188행(2.9%)** 이 있어, 행으로 세면
  접수가 2,062로 잡히고 분모가 부풀어 전환율이 전부 낮아진다.
- **사람(`피검자ID`)이 아니다.** 지금 데이터는 피검자 1인 = 검진건 1건이라 값이 같지만,
  재검이 생기면 한 사람이 검진건 2건을 갖게 되어 갈라진다.

| 파일 | 그레인 | 행 |
|---|---|---:|
| `data/검진건.csv` | 검진 건 1회 | 2,004 |
| `data/검진이벤트.csv` | 검진 건 × 단계 도달 1건 | 6,468 |

기간은 `config.PERIOD` = **2025-09-01 ~ 2026-08-31** (`검진건.의뢰일` 기준).

---

## 퍼널 — 4단계

`config.FUNNEL_STEPS` 가 원본이다. 순서가 곧 단계 순서다.

| # | 단계 | 도달 | 전 단계 대비 | 누적 |
|---|---|---:|---:|---:|
| 1 | 검진의뢰접수 | 2,004 | — | 100.00% |
| 2 | 방문예약확정 | 1,794 | 89.52% | 89.52% |
| 3 | 검진수행완료 | 1,525 | 85.01% | 76.10% |
| 4 | **결과전항목수신** | **957** | **62.75%** ← 병목 | 47.75% |

**회신은 단계가 아니다.** 전건에 회신이 오므로 이탈이 없다.

### 각 단계의 도달 조건

현재 코드가 쓰는 조건은 하나다 — **`검진이벤트` 에 그 단계 이름의 행이 있으면 도달**
(`검진건ID` 고유 기준). 앞 단계 부분집합이 4단계 전부에서 성립하는 것은 확인했다.

⚠ **단계별 업무 정의는 미정.** 특히 "결과 **전항목** 수신"에서 *전항목*의 기준이 무엇인지,
일부 항목만 온 건을 어떻게 볼지 정하지 않았다. 지금은 이벤트 행의 유무만 본다.

⚠ **`PERIOD` 로 자를 것인가 미정.** 현재 `funnel()` 은 **자르지 않는다.**
자르면 1,791 / 1,505 / **916** 이 되고, 결과수신이 957에서 **41건 줄어든다.**
이 값은 주지표의 분모라서 심사유의율도 함께 바뀐다.

---

## 지표

### 주지표

**심사유의율 = 심사유의 건 ÷ 결과 수신 건**

- 실측 **36.15%** (346 / 957) · 95% CI 33.11~39.29%
- 분자는 **검진 측 판정**(`검진건.심사유의여부`)이다. 보험사 인수 판정은 쓰지 않는다.
- 분모는 **결과 전항목 수신 건**이다.

### 가드레일

| | 지표 | 계산식 | 실측 |
|---|---|---|---|
| ① | 경계선 비율 | 경계선 건 ÷ 심사유의 건 | 22.83% (분모 346) |
| ② | 건당 유의 항목 수 | **미정** | 미실측 |
| ③ | 검진기관별 유의율 분산 | **미정** | 미실측 |
| ④ | 재방문 출장비 | **미정** — 금액 미정. 손익분기가 아니라 **예산 한도**형이다 | 미실측 |

⚠ **경계선 폭은 잠정 ±10%.** 확정하면 ①의 값(22.83%)이 바뀐다.

`metrics.kpis()` 가 화면에 올리는 것은 **심사유의율 · 경계선 비율 · 재작업률 셋**이다.
②③④는 계산식이 없어 넣지 않았다. `metrics.monthly()` 는 **월이 아니라 분기**로 자른다.

### 유지 / 이탈 (명세 5행)

**재작업률 = 결과 수신 후 90일 이내 재검 발생 건 ÷ 관측 창이 닫힌 결과 수신 건**

- 실측 **9.94%** (69 / 694)
- ⚠ 이름이 "이탈률"이 아니라 **"재작업률"** 인 이유는 **부호가 반대**이기 때문이다.
  검진에서 재검은 나쁜 신호다.
- 관측 창 90일이 안 닫힌 건은 NULL이다. **최근 3개월은 통째로 판정할 수 없다.**

### 유지 퍼널 (`config.RETENTION_STEPS`)

| 단계 | 조건 | 건수 | 전 단계 대비 |
|---|---|---:|---:|
| 결과 수신 | 결과 전항목 수신 이벤트 있음 | 957 | — |
| 90일 경과 — 판정 가능 | `재검_90일내` 가 NULL이 아님 | 694 | 72.52% |
| 재검 없음 — 유지 | `재검_90일내 = False` | 625 | 90.06% |

앞 단계 위반 **0건 · 0건** — 진짜 퍼널이다.

⚠ 둘째 단계는 업무 사건이 아니라 **관측 창**이다. 여기서 빠지는 27.48%는
**이탈이 아니라 "아직 모르는 것"** 이다. 이 단계를 빼면 재작업률이
9.94%(69/694)가 아니라 7.21%(69/957)로 낮게 보인다.

⚠ 이 셋은 **Claude 추천안**이다. 확정하려면 사용자가 검토해야 한다.

---

## 임계값과 근거

### 심사유의율 (양방향)

| | 아래쪽 | 위쪽 |
|---|---|---|
| 경고 | 29% 미만 | 43% 초과 |
| 위험 | 25% 미만 | 47% 초과 |

**근거** — 헛경보율이 4.0%가 되도록 잡은 값이다. 양방향인 이유는, 유의율이 낮아지는 것도
높아지는 것도 판정 운영이 흔들렸다는 신호이기 때문이다.
**판정은 분기 단위로 한다** — 월 수신이 66~100건뿐이라 월별 변동(26.9~48.1%)이 전부 표본
노이즈다(동질성 p=0.271).

`config.THRESHOLDS` 에 들어가 있고, `metrics.status_of()` 가 **양쪽 경계를 모두 읽는다.**
위쪽 경계는 `경고_상한`·`위험_상한` 키로 적는다.

### 최소 표본

`config.MIN_SAMPLE = 200`

**근거** — 명세 4행의 "분기 최소 200건". 이 밑으로는 비율을 내도 신뢰구간이 너무 넓어
판정에 쓸 수 없다. 검증 규칙 1(행 수)이 이 값을 차단 기준으로 쓴다.

### 가드레일 임계값

**전부 미정.** ②③④의 계산식이 정해지지 않아 임계값도 정할 수 없다.

---

## 유효 구간 — 지표마다 다르다

데이터 종료일은 **2026-09-19**(이벤트 최대 일자)다. 시작 쪽은 셋 다 2025-09-01 —
검진건 2,004건 전부가 접수 이벤트를 창 안에 갖고 있어 **좌측 절단이 없다.**

| 지표 | 신뢰 구간 | 기준 축 | 왜 거기서 잘리나 |
|---|---|---|---|
| 퍼널 전환율 | 2025-09-01 ~ **2026-08-16** | 의뢰일 | 의뢰→수신 **최대 34일**. 종료일에서 34일을 뺀 날까지만 "갈 시간이 있었다" |
| 심사유의율 | 2025-09-01 ~ **2026-09-19** | 수신일 | 판정이 **수신 시점에 확정**된다. 기다릴 시간이 없으므로 자르지 않는다 |
| 재작업률 | 2025-09-01 ~ **2026-06-21** | 수신일 | 재검 관측 창 **90일**. 그 뒤 수신 건은 재검 여부가 아직 정해지지 않았다 |

⚠ **분기 판정은 2025Q4부터**다. 2025Q3는 세 지표 모두 `MIN_SAMPLE=200` 미달이다
(퍼널 159 · 수신 25). 가장 오래된 구간이 미달이라는 점에 주의한다.

⚠ **`funnel()` 은 현재 자르지 않는다.** 자르면 결과수신이 957 → 916으로 41건 줄고
주지표의 분모가 바뀐다. 자를지는 미정이다.

## 분해 — 무엇으로 쪼개나

`config.DIMS` 가 후보다. 첫 번째가 기본값이다.

| 축 | 칸 | 최소 칸 | 격차 | p값 | 쓰는가 |
|---|---:|---:|---:|---:|---|
| **방문진단군** | 4 | 241 | **24.8%p** | 1.27e-11 | ✅ 기본 축 |
| 검진기관 | 8 | 179 | 12.2%p | 0.208 | 눌러 볼 수만 (6칸 감춰짐) |
| 연령대 | 5 | 182 | 5.6%p | 0.499 | 눌러 볼 수만 (2칸 감춰짐) |

**방문진단군을 고른 이유는 격차가 커서가 아니라 손을 쓸 수 있어서다.**
지역 등급(1군 서울·5대광역시 ~ 4군 원거리) 자체는 못 바꾸지만 배차·인력 배분은
바꿀 수 있다. 검진기관은 바꿀 수 있으나 격차가 없고, 연령대는 격차도 없고 못 바꾼다.

보는 구간은 **병목**이다 — 검진수행완료 → 결과전항목수신.
1군 70.40% vs 4군 45.64%. 미분류는 0건이고 칸 합이 1,525로 전체와 맞는다.

---

## 못 믿을 조건 — 걸리면 계산하지 않는다

`metrics.trust_check()` 에 있다. **조건은 셋인데 분기는 하나다.**

| 조건 | 기준 | 왜 그 값인가 |
|---|---|---|
| 표본이 모자란다 | `MIN_SAMPLE` **200건** 미만 | 명세 4행 "분기 최소 200건". 칸 크기(179~581)로 검증 — 150이면 아무것도 안 걸리고 250이면 검진기관이 전부 걸린다 |
| 기간이 안 찼다 | `MIN_DAYS` **34일** 미만 | 의뢰→결과수신 최대 소요가 34일. 그만큼 안 지난 건은 "아직 갈 시간이 없는 건"이다 |
| 비교가 공정하지 않다 | 유효 구간 밖 건 혼입 | **이 도메인엔 실험이 없다.** 배정 비율 대신 이것을 본다 |

**사유에는 실제 숫자를 넣는다** — "표본 부족"이 아니라 "표본 179건 (최소 200)".

### 감출 때 지킬 것

- **못 믿으면 계산 자체를 하지 않는다.** 계산해 놓고 화면에서 빼는 것이 아니다.
  값이 변수에 들어 있으면 리포트나 로그로 새어 나간다.
- **감춘 항목에는 사유만 적고 수치는 적지 않는다.** 문서에도 넣지 않는다.
- 표본 수·기간·비중은 **조건 값**이라 감추지 않는다. 지표 값·증감·p값은 감춘다.
- 회색으로도 보여주지 않는다. 사람은 본 숫자를 기억한다.

---

## 판정 — 순서가 곧 설계다

`metrics.judge()` 가 **직전 분기 대비 최근 분기**를 판정한다. 실험이 없으므로 구간 비교다.

```
1. 믿을 수 있는가      -> 아니면 여기서 끝. 계산 안 함        판정 보류 (block)
2. 주지표가 움직였는가  -> 아니면                              차이 없음 (none)
3. 가드레일은 괜찮은가  -> 나빠졌으면                          주의 필요 (warn)
4. 다 통과            ->                                    주목할 만함 (ok)
```

**3번을 2번 뒤에 두는 것이 핵심이다.** 주지표가 좋으면 거기서 멈추고 싶어지는데
코드는 멈추지 않는다.

| | 지표 | 기준 | 근거 (평소 변동 폭, 2025Q4~) |
|---|---|---|---|
| 주지표 | 심사유의율 | **5%p** 이상 움직임 | 분기간 변동 중앙 3.56%p · 최대 5.15%p |
| 가드레일① | 경계선 비율 | **3%p** 이상 상승 | 중앙 0.76%p · 최대 3.26%p |
| 가드레일② | 재작업률 | **5%p** 이상 상승 | 중앙 2.98%p · 최대 5.57%p |

주지표가 **양방향**이라 "성공"이라 부르지 않고 **"주목할 만함"** 이라 한다.
올라간 것도 내려간 것도 판정 운영이 흔들렸다는 신호일 뿐, 그 자체로 좋은 것이 아니다.

⚠ 현재 판정은 `2026Q2 → 2026Q3` **주목할 만함**이지만, **가드레일 재작업률을
확인할 수 없다** (관측 창 90일이 안 닫힘). 카드에 그 사실을 적는다 —
비워두고 성공이라 하지 않는다.

### 색

`config.COLORS` 에서 가져다 쓰고 새로 만들지 않는다.
`ok` 정상 · `warn` 주의 · `block` 차단·무효 · `none` 없음·부족.
**강조 색을 장식에 쓰지 않는다.** 색 + 기호(○ ▲ ✕ ●) + 글자를 같이 써서
색을 못 봐도 판정이 전달되게 한다.

---

## 코드를 고칠 때 지킬 것

- **지표 정의는 `core/config.py` 에서만 바꾼다.** 계산 파일에 값을 박지 않는다.
- **계산 경로에 현재 시각을 넣지 않는다.** 같은 입력이면 같은 결과가 나와야 한다.
- **시키지 않은 파일은 고치지 않는다.**

---

## 제안서 (`report/proposal.py`)

읽는 사람은 **팀장**이고, 읽고 나서 `config.PROPOSAL_DECISIONS` 중 하나를 정한다.
절 순서는 그 결정에 필요한 차례다 — 1~3 으로 "클 만한 건인가"가 서고,
4 에서 "이 숫자가 얼마나 단단한가"가 갈린다. 단단하면 승인, 흔들리면 5 의
철회 기준을 달아 조건부 승인, 4 가 무너지면 보류다.

### 절 목록 — `config.PROPOSAL_SECTIONS` 가 원본이다

| # | 제목 | 답하는 질문 | 채우는 함수 | |
|---|---|---|---|---|
| 1 | 어느 단계에서 얼마나 빠지고 있습니까? | 읽을 가치가 있는 건인가 | `metrics.topic_evidence()["현황"]` | **자동** |
| 2 | 그 이탈이 어디에 몰려 있습니까? | 손을 댈 대상이 특정되는가 | `…["원인"]` (`funnel_by()`) | **자동** |
| 3 | 고치면 연간 몇 건을 되찾습니까? | 들일 가치가 있는 크기인가 | `…["규모"]` | **자동** |
| 4 | 이 숫자를 믿어도 됩니까? | 지금 결정해도 되나 | `…["원인"]`·`…["추세"]`의 표본 | **자동** |
| 5 | 틀렸다면 무엇 때문이고, 언제 접습니까? | 조건부 승인의 조건 | — | **사람** |
| 6 | 무엇을 결정해 주셔야 합니까? | 셋 중 무엇을, 무엇에 대해 | — | **사람** |

- **사람이 쓰는 절은 둘뿐이다 — 위험·철회 기준 / 요청.** 늘리지 않는다.
  해석과 요청을 자동으로 쓰는 순간 책임이 사라진다.
- 6 절에는 **자동으로 붙는 줄**이 있다(규모 · 결정 선택지 · 미루면 쌓이는 양).
  `"문장"`(사람)과 `"자동"`(붙인 것)을 **다른 칸**에 담는다. 한 칸에 담으면
  누가 한 말인지 갈리지 않고, 사람 글에 자동 검사가 걸린다.

### 절을 추가·삭제할 때

> **이 절을 채울 함수 이름을 지금 댈 수 있는가.**

못 대면 **만들지 않는다.** "나중에 채우자"로 만든 절은 빈 채로 문서에 남고,
빈 절은 "해당 없음"으로 읽힌다. 이 도메인에 없는 것은
`## 아직 안 정한 것` 이 아니라 **없다고 적는다** — 비용·ROI·일정·실험은 없다.

**빈 절은 그리지 않는다.** 대신 어느 질문이 답 없이 남았는지를 문서 끝에 한 줄로 남긴다.
자동 절은 재료가 없으면 아예 안 만들고, 그 사유를 `proposal.missing()` 이 따로 낸다.

### 그래프 — `viz/proposal_charts.py`

- **그림 하나에 대응 문장 하나.** 본문이 말하지 않는 그림은 넣지 않는다.
  그림만 있고 문장이 없으면 읽는 사람이 무엇을 보라는 것인지 모른다.
- **색은 3개까지** — 강조 1 · 기본 1 · 회색 1. 색에는 **글자 딱지를 같이 붙인다**
  (`병목`·`최고`·`최저`). 흑백 인쇄와 색각 이상에서 색만 남으면 못 읽는다.
- **값이 없는 계열은 그리지 않는다.** 0 으로 그리면 "0 이다"가 된다.
  못 믿을 값도 안 그린다 — 사람은 본 숫자를 기억한다.
- caption 에 **그레인**(무엇을 하나로 셌는지)을 한 줄 적는다.

### 문서에 나가는 말

- **`config.PROPOSAL_WORDS` 에서만 바꾼다.** 코드에 박지 않는다.
  절 제목 · 분류어 · 판정어 · 확인 필요 · 값 구분이 전부 거기 있다.
- **왼쪽(키)은 내부 분류다. 바꾸지 않는다.** 카드 파일과 계산 코드가 그 글자로 맞춘다.
  오른쪽(값)만 바꾼다.
- 라벨에 없는 키가 오면 **에러를 내지 않고 키를 그대로 쓴다**(`proposal.말()`).
  말을 고르는 동안에도 문서는 나와야 한다.

### 검사

- **문장 검사는 `sections.check_phrasing()` 을 재수입해 쓴다. 새로 만들지 않는다.**
  `BANNED` 도 마찬가지다. 두 벌이 되면 리포트에서 막은 표현이 제안서에서 통과한다.
- **자동으로 쓴 부분에만 건다**(`proposal.자동_검사()`). 사람 글에 걸면
  사람 글 때문에 문서 조립이 멈춘다. 사람 글은 사람이 책임진다.
- 요청 문장에 결정을 요구하는 말이 없으면 **경고만 한다. 저장은 막지 않는다** —
  쓰다 만 글을 못 저장하게 하면 사람이 화면을 떠나고, 떠나면 영영 안 쓴다.
- **모르는 것은 낱말 하나로 두지 않는다.** 셋으로 적는다 —
  무엇을 모르는가 · 누가·어떻게 확인하는가 · 모르는 채로 할 수 있는 결정.

> `report/proposal_v1.py` 는 **보존해 둔 옛 판**이다. 절 구조가 다르다.
> 따라 고치지 않는다.

---

## 아직 안 정한 것 (한자리에)

| | 미정 | 정하면 무엇이 바뀌나 |
|---|---|---|
| 1 | 퍼널을 `PERIOD` 로 자를 것인가 | 결과수신 957 → 916. **주지표 분모가 바뀐다** |
| 2 | "결과 전항목 수신"의 *전항목* 기준 | 4단계 도달 조건 |
| 3 | 경계선 폭 (잠정 ±10%) | 가드레일 ① 값 (22.83%) |
| 4 | 가드레일 ②③④의 계산식 | 그 지표들과 임계값 전부 |
| 5 | 확인용 `st.caption` 3개 삭제 | 브라우저에서 조각 격리를 확인한 뒤 지운다 |
| 6 | 유지 퍼널 3단계 **확정** | 지금은 Claude 추천안이 들어가 있다 |
