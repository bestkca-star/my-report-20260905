# 8주차 Day4 — 프롬프트 13개 치환본

**대상 앱**: `c:\WORK\모두의연구소\실습\0826실습\my-report` (방문검진 심사유의)

교안 본문은 통신사 기준이고, 아래는 **내 도메인으로 바꿔 쓴 것**이다.
각 프롬프트를 **원본 → 치환** 순서로 위아래에 붙여 두었다.

## 치환에 쓴 실제 값 (기억으로 적지 않고 코드에서 읽었다)

| 항목 | 값 |
|---|---|
| `metrics.kpis()` 지표 이름 | **심사유의율 36.15% · 경계선 비율 22.83% · 재작업률 9.94%** |
| 그레인 | 검진 건 1회 · `검진건ID` 로 고유하게 (`nunique`) |
| `config.PERIOD` | 2025-09-01 ~ 2026-08-31 · `MIN_SAMPLE` 200 |
| 검증 warn 2건 | 날짜 범위(기간 뒤 65행) · 조인 안전성(완전중복 188행) |
| 감춘 항목 | 검진기관 **6/8칸** · 연령대 **2/5칸** · 전후 비교 4장 중 **1장 무효**(2025Q3 25건) |
| 가드레일 확인 불가 | 2026Q2 → 2026Q3 **재작업률** (관측 창 90일 미마감) |
| 실험 | **없음** → 5장을 전후 비교 장으로 대체 |

---

## 1 — 요약과 방법

**원본 (교안)**

```
report/sections.py 의 _s1_summary() 와 _s3_method() 를 채워줘.

요약에는 이 값들을 넣어줘: [ 지표 이름들 ]
방법에는 반드시 분석 단위(그레인)를 밝혀줘. 수요일에 정한 그것이다.

★ 인과를 단정하는 표현을 쓰지 마. "A가 낮다" 까지만.
```

**치환 (내 도메인)**

```
report/sections.py 의 _s1_summary() 와 _s3_method() 를 채워줘.

요약에는 이 값들을 넣어줘:
  심사유의율 · 경계선 비율 · 재작업률
각각 현재값과 직전 분기 대비 변화를 한 줄씩.
metrics.kpis() 와 metrics.monthly() 가 돌려주는 값을 그대로 써. 다시 계산하지 마.
가장 낮은 구간(병목: 검진수행완료 → 결과전항목수신 62.75%)도 한 줄 넣어줘.

방법에는 반드시 분석 단위(그레인)를 밝혀줘 —
  검진 건 1회, 검진건ID 로 고유하게(nunique) 센다. 사람(피검자ID)이 아니다.
그리고 아래도 적어줘. 이건 계산 방법이지 한계가 아니다.
  - 데이터 기간은 config.PERIOD 에서 읽어 쓴다 (하드코딩 금지)
  - 유효 구간이 지표마다 다르다: 퍼널은 의뢰일 2026-08-16까지,
    재작업률은 수신일 2026-06-21까지, 심사유의율은 자르지 않는다
  - 판정은 분기 단위다. 월별 변동은 표본 노이즈다
  - 재작업률의 분모는 관측 창 90일이 닫힌 건뿐이다

★ 인과를 단정하는 표현을 쓰지 마. "A가 낮다" 까지만.
```

> **바꾼 것** — 지표 이름 3개를 실제 `kpis()` 에서 읽어 넣었다. 그레인 한 줄과
> **지표별 유효 구간 3종**을 방법 장에 넣게 했다(교안: "보정을 걸었으면 방법 장에 적으라").

---

## 2 — 결과와 실험

**원본 (교안)**

```
_s4_results() 와 _s5_experiments() 를 채워줘.

결과: 단계별 값과 분해 결과를 나열한다. 해석은 넣지 마.
      "왜" 가 들어가면 그건 해석이니까 6장으로 미룬다.

실험: ★ 어제 못 믿을 조건에 걸린 항목은 사유만 적고 수치를 쓰지 마.
      화면에서 감춘 숫자를 문서에 쓰면 감춘 의미가 없다.
```

**치환 (내 도메인)**

```
_s4_results() 와 _s5_experiments() 를 채워줘.

결과: 아래를 나열한다. 해석은 넣지 마. "왜" 가 들어가면 6장으로 미룬다.
  - 퍼널 4단계 값과 전환율 (2,004 → 1,794 → 1,525 → 957)
  - 분해 결과: 방문진단군 4칸의 도달·전환율·비중
  - 유지 퍼널 3단계

실험 장은 없다. 이 도메인엔 무작위 배정 실험이 없다.
_s5_experiments() 를 전후 비교 장으로 바꿔줘. metrics.judge_pairs() 를 쓴다.
  - 무작위 배정이 없었으므로 인과를 주장할 수 없다는 문장을
    그 장의 첫 문단에 넣어줘. 각주 말고 본문에.
  - ★ 못 믿을 조건에 걸린 카드(2025Q3 → 2025Q4)는 사유만 적고 수치를 쓰지 마.
    화면에서 감춘 숫자를 문서에 쓰면 감춘 의미가 없다.
  - 가드레일을 확인하지 못한 카드는 그 사실을 적어줘.
    비워두고 "주목할 만함"으로만 끝내지 마.
```

> **바꾼 것** — 실험 장을 **전후 비교 장으로 대체**(교안 p9 「내 도메인이라면」).
> 인과 문구를 첫 문단에 넣게 하고, **가드레일 확인 불가를 적게** 한 줄 추가.

---

## 3 — 금지어 목록

**원본 (교안)**

```
report/sections.py 의 BANNED 에 아래를 더해줘.

  [ 내 도메인 표현들 ]

그리고 check_phrasing() 이 사람이 쓴 장에도 적용되는지 확인해줘.
안 되어 있으면 되게 해줘.
```

**치환 (내 도메인)**

```
report/sections.py 의 BANNED 에 아래를 더해줘.
기본 여섯(때문에·덕분에·효과로·입증되었·증명되었·확실히)은 이미 있다.

  원인이다 · 유발 · 기인 · 영향을 미쳤 · 개선되었음이 확인 · 시사한다

이 도메인에서 특히 위험한 이유도 주석으로 한 줄 남겨줘 —
검사 수치와 판정 결과가 같은 표에 있어서 상관을 인과로 쓰기 쉽다.
"공복혈당이 높아 심사유의가 났다"는 이 데이터로 가릴 수 없는 주장이다.

그리고 check_phrasing() 이 사람이 쓴 장(2·6·8장)에도 적용되는지 확인해줘.
안 되어 있으면 되게 해줘.
```

> **바꾼 것** — 의료·행정 성격에 맞는 6개를 골랐다. 교안 판단 기준("이 말이 상관을
> 인과로 바꾸는가")을 이 데이터에서 **어떻게 어기게 되는지**를 주석으로 남기게 했다.

---

## 4 — 검사가 잡는지 확인

**원본 (교안)**

```
자동 생성 장 하나에 일부러 인과를 단정하는 문장을 넣어줘.
예를 들면 "모바일 화면이 불편하기 때문에 완료율이 낮다" 같은 것.

넣고 나서 리포트 화면에서 경고가 뜨는지 확인하고,
확인이 끝나면 원래대로 되돌려줘.
```

**치환 (내 도메인)**

```
자동 생성 장 하나에 일부러 인과를 단정하는 문장을 넣어줘.
예를 들면 "4군은 이동 거리가 멀기 때문에 결과 수신율이 낮다" 같은 것.
새로 더한 금지어도 걸리는지 보게 하나 더 넣어줘 —
"검사 수치 이상이 심사유의를 유발했다".

넣고 나서 리포트 화면에서 경고가 뜨는지 확인하고,
어떤 장의 어떤 단어가 걸렸는지 보이는지도 확인해줘.
확인이 끝나면 원래대로 되돌려줘.
```

> **바꾼 것** — 예문을 내 도메인 문장으로. **기본 금지어 하나 + 새로 더한 것 하나**를
> 같이 넣어 둘 다 잡히는지 보게 했다.

---

## 5 — 한계 조립  ★ 오늘의 핵심

**원본 (교안)**

```
report/sections.py 의 _s7_limits() 를 채워줘.

★ 사람이 매번 쓰는 것이 아니라 자동으로 조립되게 해줘.

1. validate.run_checks() 에서 level 이 warn 인 항목을 전부 가져온다
2. 어제 못 믿을 조건에 걸려 판정하지 않은 항목을 넣는다
   — 무엇을, 왜 판정하지 않았는지
3. 아래 두 문장은 항상 넣는다
   - 관측 데이터이므로 인과를 주장할 수 없다
   - 기간이 [ 기간 ] 이므로 그보다 긴 주기의 변화는 관측되지 않는다
4. 내가 직접 적을 것도 넣어줘:
   [ 이번 분석에서 확인하지 못한 것 ]
```

**치환 (내 도메인)**

```
report/sections.py 의 _s7_limits() 를 채워줘.

★ 사람이 매번 쓰는 것이 아니라 자동으로 조립되게 해줘.

1. validate.run_checks() 에서 level 이 warn 인 항목을 전부 가져온다
   (지금 2건: 날짜 범위 · 조인 안전성)
2. 못 믿을 조건에 걸려 판정하지 않은 항목을 넣는다 — 무엇을, 왜
   - metrics.funnel_by() 의 사유 컬럼에서 축별로 모은다
   - metrics.judge_pairs() 에서 판정이 "무효"인 카드
   - 가드레일 중 "확인불가"인 것도 넣는다
   ★ 사유와 표본 수만 적고 전환율·증감은 적지 마. 계산하지 않은 값이다.
3. 아래 두 문장은 항상 넣는다
   - 관측 데이터이므로 인과를 주장할 수 없다
   - 기간이 config.PERIOD 이므로 그보다 긴 주기의 변화는 관측되지 않는다
     (하드코딩하지 말고 config 에서 읽어 써)
4. 내가 직접 적을 것도 넣어줘:
   - 검사 항목이 공복혈당·총콜레스테롤·ALT 셋뿐이라, 그 밖의 항목이
     심사유의 판정에 어떻게 쓰였는지는 이 데이터로 볼 수 없음
   - 재검이 별도 검진건으로 없고 플래그(재검_90일내)로만 있어,
     재검 이후의 경과는 추적하지 못함
   - 검진기관별·연령대별 격차를 확인했으나 신호가 없었음
     (검진기관 p=0.208 · 연령대 p=0.499). 방문진단군만 유의(p=1.27e-11)
```

> **바꾼 것** — 2번을 **세 군데**(분해 칸·무효 카드·확인불가 가드레일)에서 모으게 했고,
> "수치를 적지 마"를 명시했다. 4번의 셋째가 교안이 강조한 **"찾았는데 없던 것"** 이다.

---

## 6 — 한계 확인

**원본 (교안)**

```
지금 한계 절에 몇 개 항목이 들어갔는지 보여줘.
그리고 화요일 검증에서 난 경고가 전부 반영됐는지 대조해줘.
빠진 게 있으면 알려줘.
```

**치환 (내 도메인)**

```
지금 한계 절에 몇 개 항목이 들어갔는지 보여줘.
그리고 아래 넷을 각각 대조해줘. 빠진 게 있으면 알려줘.

  - validate.run_checks() 의 warn 2건이 다 들어갔는가
  - 감춘 칸(검진기관 6칸 · 연령대 2칸)이 반영됐는가
  - 무효 카드(2025Q3 → 2025Q4)와 확인 불가 가드레일(재작업률)이 들어갔는가
  - "찾았는데 없던 것"이 한 줄이라도 있는가

그리고 한계 절에 감춘 항목의 전환율·증감이 새어 들어가지 않았는지도 봐줘.
```

> **바꾼 것** — 대조 대상을 **넷으로 명시**. 마지막 줄에 "수치 유출 검사"를 더했다.

---

## 7 — PDF 만들기

**원본 (교안)**

```
PDF를 만들어줘. 만들고 나서 확인해줘.

- 한글이 깨지지 않는가
- 사람이 안 쓴 장이 있으면 "작성되지 않음" 이 찍히는가
- 차트가 들어갔는가
```

**치환 (내 도메인)**

```
PDF를 만들어줘. 만들고 나서 확인해줘.

- 한글이 깨지지 않는가 (검진·심사유의·방문진단군 같은 말이 네모로 안 나오는가)
- 사람이 안 쓴 장이 있으면 "작성되지 않음" 이 찍히는가
- 차트가 들어갔는가 (퍼널 · 방문진단군 분해)
- 감춘 항목의 수치가 PDF 안에 없는가  ← 화면에서만 감추면 의미가 없다
- 그리스 문자를 쓰지 않았는가 (α 대신 "유의수준")
```

> **바꾼 것** — 확인 항목 둘을 더했다. **수치 유출 검사**는 어제 CLAUDE.md에 박은
> "감춘 항목의 수치는 문서에도 안 넣는다"가 실제로 지켜지는지 보는 것이다.

---

## 8 — 발송 전 최종 점검

**원본 (교안)**

```
게이트 3 통과 전 점검을 해줘.

- 사람이 쓰는 장 중 비어 있는 것이 있는가
- 인과 단정 표현이 남아 있는가 (자동·사람 장 전부)
- 화요일 검증 경고가 한계 절에 다 있는가
- 어제 감춘 항목의 수치가 문서 어딘가에 새어 나오지 않았는가

하나라도 걸리면 통과하지 말라고 말해줘.
```

**치환 (내 도메인)**

```
게이트 3 통과 전 점검을 해줘.

- 사람이 쓰는 장 중 비어 있는 것이 있는가
- 인과 단정 표현이 남아 있는가 (자동·사람 장 전부)
- 검증 경고 2건이 한계 절에 다 있는가
- 감춘 항목의 수치가 문서 어딘가에 새어 나오지 않았는가
  (검진기관 6칸 · 연령대 2칸 · 2025Q3 무효 카드의 전환율·증감)
- 숫자가 어제 화면과 같은가 (심사유의율 36.15% · 경계선 22.83% · 재작업률 9.94%)

하나라도 걸리면 통과하지 말라고 말해줘.
```

> **바꾼 것** — 교안 골격은 그대로 두고 **대조할 실제 숫자**를 넣었다.
> 빈칸은 프롬프트가 아니라 아래 게이트 3 근거 문구에 있다.

**게이트 3 근거 문구 (화면에 입력할 것)**

```
초안 확정. 실제 발송 없음. 게이트 통과 기록만 남김.
보험사 담당자에게 방문진단군별 결과 수신율 격차를 보고할 초안이며,
한계 [N]건을 명시함.
```

---

## 9 — 기록 확인

**원본 (교안)**

```
게이트 1·2·3의 통과 기록이 전부 남았는지 확인해줘.
아카이브 화면에서 셋이 나란히 보이는지도.
```

**치환 (내 도메인)**

```
게이트 1·2·3의 통과 기록이 전부 남았는지 확인해줘.
아카이브 화면에서 셋이 나란히 보이는지도.

★ runs/*.json 은 .gitignore 라 git 이 지켜주지 않는다.
  기록 파일을 패턴(runs/*.json)으로 지우는 명령을 쓰지 마.
  지울 일이 있으면 파일 이름을 직접 적어서 지운다.
```

> **바꾼 것** — 경고 한 줄을 더했다. 이 세션에서 **게이트 기록을 두 번 날렸다.**

---

## 10 — 세 장을 폼으로

**원본 (교안)**

```
리포트 화면에서 사람이 쓰는 2·6·8장을 st.form 하나로 묶어줘.

- with st.form("사람이 쓰는 장") 안에 st.text_area 셋
- 각 칸에 placeholder 로 "무엇을 적는 칸인지" 한 줄씩
- st.form_submit_button("저장") 을 누를 때만 저장된다
- 저장 후 금지어 검사를 돌리고, 걸리면 어느 장 어느 단어인지 보여준다
- 걸려도 저장은 한다. 다만 경고를 남긴다

기존 값이 있으면 text_area 의 value 로 채워줘.
```

**치환 (내 도메인)**

```
리포트 화면에서 사람이 쓰는 2·6·8장을 st.form 하나로 묶어줘.

- with st.form("사람이 쓰는 장") 안에 st.text_area 셋
- placeholder 는 이렇게 —
    배경: 왜 이 분석을 했는가. 어떤 결정을 앞두고 있는가
          (예: 방문진단군별 배차·인력 배분을 조정할지 판단하려고)
    해석: 숫자가 무엇을 뜻하는가. 원인을 단정하지 말고,
          무엇을 이 데이터로 가릴 수 없는지도 함께 적는다
    제안: 무엇을 할 것인가, 무엇을 하지 않을 것인가
- st.form_submit_button("저장") 을 누를 때만 저장된다
- 저장 후 check_phrasing() 을 돌리고, 걸리면 어느 장 어느 단어인지 보여준다
- 걸려도 저장은 한다. 다만 경고를 남긴다

기존 값이 있으면 text_area 의 value 로 채워줘.
```

> **바꾼 것** — placeholder를 내 도메인 문장으로. 해석 칸에 **"무엇을 가릴 수 없는지도
> 적으라"** 를 넣었다(교안 p14의 좋은 해석 예시).

---

## 11 — 한계 편집기

**원본 (교안)**

```
한계 절을 st.data_editor 로 편집할 수 있게 만들어줘.

컬럼은 셋이다.
- 출처: SelectboxColumn ["검증 경고", "못 한 것", "찾았는데 없음"]
- 내용: TextColumn (필수)
- 포함: CheckboxColumn (기본 True)

- num_rows="dynamic" 으로 행을 추가·삭제할 수 있게
- 검증에서 조립된 행은 미리 채워 넣는다
- "포함" 체크를 끈 행은 리포트에 안 들어간다
- 편집 결과를 세션에 저장하고, PDF 만들 때 그걸 쓴다

검증에서 온 행을 지워도 되게 하되,
지우면 "검증 경고를 뺐습니다" 라고 화면에 남겨줘.
```

**치환 (내 도메인)**

```
한계 절을 st.data_editor 로 편집할 수 있게 만들어줘.

컬럼은 셋이다.
- 출처: SelectboxColumn ["검증 경고", "못 한 것", "찾았는데 없음"]
- 내용: TextColumn (필수)
- 포함: CheckboxColumn (기본 True)

- num_rows="dynamic" 으로 행을 추가·삭제할 수 있게
- 미리 채워 넣을 행:
    검증 경고 — 날짜 범위 · 조인 안전성 (2건)
    못 한 것 — 검진기관 6칸 · 연령대 2칸 · 2025Q3 무효 · 재작업률 확인 불가
    찾았는데 없음 — 검진기관 p=0.208 · 연령대 p=0.499
- "포함" 체크를 끈 행은 리포트에 안 들어간다
- 편집 결과를 세션에 저장하고, PDF 만들 때 그걸 쓴다

검증에서 온 행을 지워도 되게 하되,
지우면 "검증 경고를 뺐습니다" 라고 화면에 남겨줘.
```

> **바꾼 것** — 미리 채울 행을 **출처 셋으로 나눠 명시**했다.
> 교안이 "찾았는데 없음은 대개 비어 있다"고 했는데, 내 것은 이미 둘 있다.

---

## 12 — 진행 표시와 알림

**원본 (교안)**

```
PDF 만들기를 st.status 로 감싸서 단계별로 보여줘.

with st.status("리포트를 만드는 중", expanded=True) as box:
    1) 장별 내용 모으는 중
    2) 차트 이미지 만드는 중
    3) PDF 조립하는 중
  각 단계마다 st.write 로 한 줄씩 남긴다
  끝나면 box.update(label="완성", state="complete", expanded=False)
  실패하면 state="error" 로 두고 무엇이 실패했는지 남긴다

끝난 뒤 st.toast("리포트가 만들어졌습니다", icon="📄") 를 띄우고,
st.download_button 으로 바로 받을 수 있게 해줘.
파일 이름에 실행 시각을 넣어줘.
```

**치환 (내 도메인)**

```
PDF 만들기를 st.status 로 감싸서 단계별로 보여줘.

with st.status("리포트를 만드는 중", expanded=True) as box:
    1) 장별 내용 모으는 중
    2) 차트 이미지 만드는 중 (퍼널 · 방문진단군 분해)
    3) PDF 조립하는 중
  각 단계마다 st.write 로 한 줄씩 남긴다
  끝나면 box.update(label="완성", state="complete", expanded=False)
  실패하면 state="error" 로 두고 무엇이 실패했는지 남긴다

아직 안 채운 계산이 있으면 그 차트만 빼고 조립하고,
무엇이 빠졌는지 st.write 로 남겨줘. (지금 build_pdf 가 그렇게 되어 있다)

끝난 뒤 st.toast("리포트가 만들어졌습니다", icon="📄") 를 띄우고,
st.download_button 으로 바로 받을 수 있게 해줘.
파일 이름에 실행 시각을 넣어줘.
★ 파일 이름의 시각은 화면 표시용이다. 계산 경로에는 현재 시각을 넣지 마.
```

> **바꾼 것** — "빠진 차트를 남기라"를 추가(이미 그렇게 만들어 둔 것과 맞춤).
> 교안 경고(계산 경로에 시각 금지)를 프롬프트 본문으로 올렸다.

---

## 13 — 판단 기준 한 장

**원본 (교안)**

```
오늘 배운 것 중 도메인과 무관한 문장만 남겨 한 장으로 정리해줘.

- 특정 업종·데이터 이름이 남아 있으면 빼줘
- 내가 오늘 내린 결정도 한 줄씩
  (어느 장을 사람이 쓰기로 했고 왜, 무슨 표현을 금지했고 왜)
- my-report/판단기준.md 에 날짜와 함께 덧붙여줘. 덮어쓰지 마

그리고 화~금 4일치를 다 모아서 중복을 정리해줘.
같은 말이 두 번 나오면 하나로 합쳐줘.
```

**치환 (내 도메인)**

```
오늘 배운 것 중 도메인과 무관한 문장만 남겨 한 장으로 정리해줘.

- 특정 업종·데이터 이름이 남아 있으면 빼줘
- 내가 오늘 내린 결정도 한 줄씩
  (어느 장을 사람이 쓰기로 했고 왜, 무슨 표현을 금지했고 왜)
- Claude가 말 안 하고 정한 것 중 무엇을 잡아냈는지도 남겨줘
- 아직 안 정한 것은 "안 정했다"고 적어줘. 정한 것처럼 적지 마
- my-report/판단기준.md 에 날짜와 함께 덧붙여줘. 덮어쓰지 마

그리고 화~금 4일치를 다 모아서 중복을 정리해줘.
같은 말이 두 번 나오면 하나로 합치되, 어느 날 것인지는 남겨줘.
정리 전후로 문장이 몇 개였는지 알려줘.
```

> **바꾼 것** — 앞 이틀에 넣었던 두 줄(★ 항목 · "안 정한 것은 안 정했다고")을 유지.
> 중복 정리에서 **날짜 출처를 남기게** 하고 **정리 전후 개수**를 요구했다.

---

## 빈칸을 어떻게 채웠나 — 교안 대비 요약

| # | 교안의 빈칸 | 내가 넣은 것 |
|---|---|---|
| 1 | `[ 지표 이름들 ]` | 심사유의율 · 경계선 비율 · 재작업률 (코드에서 읽음) |
| 3 | `[ 내 도메인 표현들 ]` | 원인이다 · 유발 · 기인 · 영향을 미쳤 · 개선되었음이 확인 · 시사한다 |
| 5 | `[ 기간 ]` | `config.PERIOD` 에서 읽게 (하드코딩 금지) |
| 5 | `[ 이번 분석에서 확인하지 못한 것 ]` | 검사 항목 3개뿐 · 재검 추적 불가 · **격차 찾았으나 없었음** |
| 8 | `[대상] [무엇] [N]` | 보험사 담당자 · 방문진단군별 결과 수신율 격차 · 한계 N건 |

**2·9·10·11·12번은 교안에 빈칸이 없다.** 그래도 바꾼 것은, 이 앱에 이미 만들어 둔 것
(`judge_pairs`·`build_pdf` 의 빠진 차트 처리)과 맞추고, 이 세션에서 실제로 낸 사고
(기록 파일 삭제)를 막기 위해서다.

---

## 오늘 프롬프트에 없는 것 — 부록 D (이메일 발송)

교안 13개에는 없고 부록 D에 발송 프롬프트가 하나 더 있다.
**수업 중에 안 해도 된다**고 되어 있고, 하려면 `.streamlit/secrets.toml` 과
Gmail 앱 비밀번호가 필요하다. `.gitignore` 에 `secrets.toml` 이 들어 있는 것은
확인했다. 할지 말지는 **정하지 않았다.**
